import React from "react";

export interface IPredictionsProps {};

export const Predictions: React.FunctionComponent<IPredictionsProps> = props => {
    return (
        <div>
            <h1>Predictions</h1>
        </div>
    )
}