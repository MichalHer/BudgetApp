import React from "react";

export interface IAccountsProps {};

export const Accounts: React.FunctionComponent<IAccountsProps> = props => {
    return (
        <div>
            <h1>Accounts</h1>
        </div>
    )
}