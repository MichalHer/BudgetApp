import React from "react";

export interface ITransfersProps {};

export const Transfers: React.FunctionComponent<ITransfersProps> = props => {
    return (
        <div>
            <h1>Transfers</h1>
        </div>
    )
}